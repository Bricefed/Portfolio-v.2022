const SectionServices = ({ title, description }) => {
  return (
    <div>
      <h4>{title}</h4>
      <p className="description">{description}</p>
    </div>
  );
};

export default SectionServices;
